name: enterprise_bi_project
version: "1.0.0"
config-version: 2

profile: enterprise_bi_project

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  enterprise_bi_project:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: mart
      +materialized: table
